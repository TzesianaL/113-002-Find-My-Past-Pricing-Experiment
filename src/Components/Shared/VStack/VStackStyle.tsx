import styled from 'styled-components'

/**
 * Styled components are used in order to style VStack component. Props are passed through ThemeProvider.
 */
export const VStackStyle = styled.div`
  display: flex;
  flex-direction: column;
`
